#Visualizations

##Augmented Feature Importances

This bar chart, generated by the intermediate Random Forest model, visualizes the relative contribution of each engineered feature (amino acid composition, charge, hydrophobicity, etc.) in classifying the proteins. It shows which features were ultimately selected for the final SVM training.

<img src="svm_augmented_feature_importances.png" alt="Augmented Feature Importances from RandomForest" width="60%">

##Confusion Matrix on Test Set

The confusion matrix shows the number of correct and incorrect predictions made by the final optimized SVM model against the ground truth labels in the hold-out test set.

<img src="svm_confusion_matrix.png" alt="Confusion Matrix on Test Set" width="60%">

##Receiver Operating Characteristic (ROC) Curve

The ROC curve illustrates the trade-off between the True Positive Rate (Sensitivity) and the False Positive Rate (1-Specificity). The Area Under the Curve (AUC) provides a comprehensive measure of the model's ability to discriminate between positive and negative classes across all possible thresholds.

<img src="svm_roc_curve.png" alt="Receiver Operating Characteristic Curve" width="60%">
